[project]
name = "bllm"
version = "0.0.1"
description = "Companion for the book - Build a Large Language Model (From Scratch)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff>=0.8.5",
]
